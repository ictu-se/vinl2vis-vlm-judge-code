# Documentation

This folder contains protocol notes for the VLM-as-judge study.

- `EXPERIMENT_PROTOCOL.md`: judge-tiered audit protocol and validation boundary.
- `EXPERIMENT_PLAN.md`: experiment plan used while preparing the paper.

The annotation package metadata is stored under `../calibration/annotation_package/`.
